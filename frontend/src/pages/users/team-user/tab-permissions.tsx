import { TabsContent } from "@/components/ui/tabs";

export default function TeamPermissionsTab({ value }: { value: string }) {
  return <TabsContent value={value}>Permissions Content</TabsContent>;
}
